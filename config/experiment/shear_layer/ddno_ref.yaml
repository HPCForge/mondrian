name: shear_layer

data_path: '/pub/afeeney/data/data'

train_cfg:
  batch_size: 16
  max_epochs: 1000

model_cfg:
  model_name: ddno_ref
  hidden_channels: 32
  layers: 2
  op_cfg:
    op_name: additive
    kernel_name: low_rank
    use_padding: False
    use_coarse_op: False
