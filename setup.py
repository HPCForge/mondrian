from setuptools import find_packages, setup

setup(
    name="mondrian-util-lib",
    packages=find_packages(include=["mondrian"]),
    version="0.0.0",
    description="Library for utilities in Mondrian",
    author="Arthur Feeney",
    install_requires=[],
)
